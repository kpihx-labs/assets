version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: kpihx-labs-assets
    restart: unless-stopped
    volumes:
      - .:/usr/share/nginx/html:ro
    networks:
      - cloudflare-tunnel

networks:
  cloudflare-tunnel:
    external: true
